APP_NAME=极客优选智能客服
APP_ENV=development
APP_DEBUG=true

# 模型密钥只保存在后端的 .env 中，不要提交到 Git，也不要传给前端。
MODEL_PROVIDER=openai
MODEL_NAME=
MODEL_API_KEY=

# 使用 OpenAI 官方接口时留空；使用 OpenAI 兼容服务时填写其 API 根地址。
MODEL_BASE_URL=
MODEL_TEMPERATURE=0.1
MODEL_TIMEOUT_SECONDS=30
MODEL_MAX_RETRIES=2
